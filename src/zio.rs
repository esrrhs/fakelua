pub(crate) struct Zio {
    // /* bytes still unread */
    // n: usize,
    // const char * p; /* current position in buffer */
    // lua_Reader reader; /* reader function */
    // void * data; /* additional data */
    // lua_State * L; /* Lua state (for reader) */
}

pub(crate) fn zgetc(z: &mut Zio) -> u32 {
    0
}
